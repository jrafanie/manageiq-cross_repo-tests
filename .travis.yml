dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=jrafanie/manageiq@rails-5-2_48e0d011_before_first_stab
  matrix:
  - TEST_REPO=jrafanie/manageiq@rails-5-2_48e0d011_before_first_stab
  - TEST_REPO=manageiq-api
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-automation_engine
  - TEST_REPO=manageiq-providers-vmware
